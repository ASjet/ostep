#include <stdio.h>
#include <stdlib.h>

////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
int main(void)
{
    int * arr = (int*)malloc(sizeof(int)*100);
    free(arr);
    printf("%d\n",arr[2]);
    free(arr+5);
    return 0;
}